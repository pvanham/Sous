# Sous

AI-assisted kitchen scheduling platform.

## Local Setup

1. Install dependencies:
   - `npm install`
2. Create a `.env.local` using the example below.
3. Run the dev server:
   - `npm run dev`

## Environment Variables

Use `.env.example` as a starting point.

